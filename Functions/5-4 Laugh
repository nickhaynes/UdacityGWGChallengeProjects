/*
 * Programming Quiz: Laugh (5-4)
 */

var laugh = function(max) {
    var giggles = "";
    for (var i = 0; i < max; i++) {
        giggles += "ha";
    }
    return giggles + "!";
}

console.log(laugh(10));
